import styled from "styled-components";

const ContainerCards = styled.div`
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  padding: 1.5rem auto;
  gap: 2rem;
`;

export { ContainerCards };
